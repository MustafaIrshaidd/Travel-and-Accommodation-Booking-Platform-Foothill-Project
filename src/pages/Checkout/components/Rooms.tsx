import React from "react";
import { Box } from "@mui/material";

const Rooms = () => {
    
    return (
        <Box gap={4} sx={{ overflowY: "scroll" }}>

            

        </Box>
    );
};

export default Rooms;
